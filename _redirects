# Cloudflare Pages: serve share fallback for Universal Link paths (URL in browser stays /s/...)
# Copy this file to the root of your deployed site (same repo as index.html).
/s/* /share-link-fallback.html 200
